<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="fastmarket.com.br.fastmarket">

    <uses-permission android:name="android.permission.SEND_SMS" />

    <application
        android:name=".activity.myApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.MainActivity">

        </activity>
        <activity
            android:name=".activity.SplashScreenActivity"
            android:theme="@style/AppCompat.TelaCheia">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".activity.LoginActivity"
            android:theme="@style/EditTheme" />
        <activity
            android:name=".activity.CadastroActivity"
            android:theme="@style/EditTheme" />

        <activity android:name=".activity.AdminActivity" />

        <activity
            android:name=".activity.EsqueciActivity"
            android:theme="@style/EditTheme" />
        <activity
            android:name=".activity.RecuperaTokenActivity"
            android:theme="@style/EditTheme" />
        <activity android:name=".activity.HistoricoActivity" />
        <activity android:name=".activity.AlteraDadosActivity" />
        <activity android:name=".Testes"></activity>
    </application>

</manifest>